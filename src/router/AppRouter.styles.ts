import { COLOR_TOKENS } from '@/theme/themeTokens';

export const styles = {
  pageLoaderWrapper: {
    height: '100vh',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },

  pageLoaderSpinner: {
    color: COLOR_TOKENS.textSecondary,
  },
};
